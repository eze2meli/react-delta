// import { selectAppPageSwaggerDomain } from '../selectors';

describe('selectAppPageSwaggerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
