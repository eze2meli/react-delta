/*
 * AppPageSwagger Messages
 *
 * This contains all the text for the AppPageSwagger container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AppPageSwagger';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AppPageSwagger container!',
  },
});
