/*
 * AppPageUsage Messages
 *
 * This contains all the text for the AppPageUsage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AppPageUsage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AppPageUsage container!',
  },
});
