// import { selectEndpointPageDomain } from '../selectors';

describe('selectEndpointPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
