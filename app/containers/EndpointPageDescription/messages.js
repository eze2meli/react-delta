/*
 * EndpointPageDescription Messages
 *
 * This contains all the text for the EndpointPageDescription container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EndpointPageDescription';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EndpointPageDescription container!',
  },
});
