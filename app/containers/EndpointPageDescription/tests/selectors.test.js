// import { selectEndpointPageDescriptionDomain } from '../selectors';

describe('selectEndpointPageDescriptionDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
