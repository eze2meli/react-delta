/*
 * AppPageDescription Messages
 *
 * This contains all the text for the AppPageDescription container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AppPageDescription';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AppPageDescription container!',
  },
});
