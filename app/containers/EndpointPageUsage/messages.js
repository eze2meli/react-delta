/*
 * EndpointPageUsage Messages
 *
 * This contains all the text for the EndpointPageUsage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EndpointPageUsage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EndpointPageUsage container!',
  },
});
